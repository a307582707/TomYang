# Task 58 — 远程分支清理清单（待确认后删除）

**状态:** 仅清单，**未删除**任何远程分支。备份标签 `backup/wiki-master-20260727` 必须保留。

## 主仓 origin — 建议删除（已合并主题分支）

| 分支 | 理由 |
|------|------|
| `archive/dangerous-manifests` | 主题已合并或过时；待确认后 `git push origin --delete archive/dangerous-manifests` |
| `audit/compat-matrix-v2` | 主题已合并或过时；待确认后 `git push origin --delete audit/compat-matrix-v2` |
| `audit/k8s-compatibility` | 主题已合并或过时；待确认后 `git push origin --delete audit/k8s-compatibility` |
| `audit/k8s-correctness` | 主题已合并或过时；待确认后 `git push origin --delete audit/k8s-correctness` |
| `audit/k8s-security` | 主题已合并或过时；待确认后 `git push origin --delete audit/k8s-security` |
| `audit/observability` | 主题已合并或过时；待确认后 `git push origin --delete audit/observability` |
| `audit/observability-completeness` | 主题已合并或过时；待确认后 `git push origin --delete audit/observability-completeness` |
| `chore/repository-audit-fixes` | 主题已合并或过时；待确认后 `git push origin --delete chore/repository-audit-fixes` |
| `ci/doc-quality` | 主题已合并或过时；待确认后 `git push origin --delete ci/doc-quality` |
| `ci/static-check-tests` | 主题已合并或过时；待确认后 `git push origin --delete ci/static-check-tests` |
| `ci/static-checks` | 主题已合并或过时；待确认后 `git push origin --delete ci/static-checks` |
| `ci/static-layers` | 主题已合并或过时；待确认后 `git push origin --delete ci/static-layers` |
| `ci/wiki-link-check` | 主题已合并或过时；待确认后 `git push origin --delete ci/wiki-link-check` |
| `docs/codeowners` | 主题已合并或过时；待确认后 `git push origin --delete docs/codeowners` |
| `docs/contributing` | 主题已合并或过时；待确认后 `git push origin --delete docs/contributing` |
| `docs/final-acceptance` | 主题已合并或过时；待确认后 `git push origin --delete docs/final-acceptance` |
| `docs/issue-templates` | 主题已合并或过时；待确认后 `git push origin --delete docs/issue-templates` |
| `docs/license-audit` | 主题已合并或过时；待确认后 `git push origin --delete docs/license-audit` |
| `docs/maintenance-inventory` | 主题已合并或过时；待确认后 `git push origin --delete docs/maintenance-inventory` |
| `docs/naming-normalization-map` | 主题已合并或过时；待确认后 `git push origin --delete docs/naming-normalization-map` |
| `docs/pr-template` | 主题已合并或过时；待确认后 `git push origin --delete docs/pr-template` |
| `docs/pr2-cleanup-record` | 主题已合并或过时；待确认后 `git push origin --delete docs/pr2-cleanup-record` |
| `docs/quarterly-review` | 主题已合并或过时；待确认后 `git push origin --delete docs/quarterly-review` |
| `docs/task32-dependency-graph` | 主题已合并或过时；待确认后 `git push origin --delete docs/task32-dependency-graph` |
| `docs/task33-ha-test` | 主题已合并或过时；待确认后 `git push origin --delete docs/task33-ha-test` |
| `docs/task34-pki` | 主题已合并或过时；待确认后 `git push origin --delete docs/task34-pki` |
| `docs/task35-rbac` | 主题已合并或过时；待确认后 `git push origin --delete docs/task35-rbac` |
| `docs/task36-podsec` | 主题已合并或过时；待确认后 `git push origin --delete docs/task36-podsec` |
| `docs/task37-resources` | 主题已合并或过时；待确认后 `git push origin --delete docs/task37-resources` |
| `docs/task38-probes` | 主题已合并或过时；待确认后 `git push origin --delete docs/task38-probes` |
| `docs/task39-scheduling` | 主题已合并或过时；待确认后 `git push origin --delete docs/task39-scheduling` |
| `docs/task41-ingress` | 主题已合并或过时；待确认后 `git push origin --delete docs/task41-ingress` |
| `docs/task42-cni-adr` | 主题已合并或过时；待确认后 `git push origin --delete docs/task42-cni-adr` |
| `docs/task43-service-dns` | 主题已合并或过时；待确认后 `git push origin --delete docs/task43-service-dns` |
| `docs/task44-pull` | 主题已合并或过时；待确认后 `git push origin --delete docs/task44-pull` |
| `docs/task45-shell` | 主题已合并或过时；待确认后 `git push origin --delete docs/task45-shell` |
| `docs/task46-dr` | 主题已合并或过时；待确认后 `git push origin --delete docs/task46-dr` |
| `docs/task47-fault` | 主题已合并或过时；待确认后 `git push origin --delete docs/task47-fault` |
| `docs/task48-release` | 主题已合并或过时；待确认后 `git push origin --delete docs/task48-release` |
| `docs/task60-etcd-runbook` | 主题已合并或过时；待确认后 `git push origin --delete docs/task60-etcd-runbook` |
| `docs/task61-images` | 主题已合并或过时；待确认后 `git push origin --delete docs/task61-images` |
| `docs/task64-security-plan` | 主题已合并或过时；待确认后 `git push origin --delete docs/task64-security-plan` |
| `docs/task65-obs-persist` | 主题已合并或过时；待确认后 `git push origin --delete docs/task65-obs-persist` |
| `docs/upgrade-readme` | 主题已合并或过时；待确认后 `git push origin --delete docs/upgrade-readme` |
| `docs/wiki-task5-ia` | 主题已合并或过时；待确认后 `git push origin --delete docs/wiki-task5-ia` |
| `docs/wiki-task6-infra` | 主题已合并或过时；待确认后 `git push origin --delete docs/wiki-task6-infra` |
| `docs/wiki-task7-archive` | 主题已合并或过时；待确认后 `git push origin --delete docs/wiki-task7-archive` |
| `examples/current-baseline` | 主题已合并或过时；待确认后 `git push origin --delete examples/current-baseline` |
| `examples/task40-netpol` | 主题已合并或过时；待确认后 `git push origin --delete examples/task40-netpol` |
| `examples/task59-nginx` | 主题已合并或过时；待确认后 `git push origin --delete examples/task59-nginx` |
| `examples/task66-baseline-fill` | 主题已合并或过时；待确认后 `git push origin --delete examples/task66-baseline-fill` |
| `feat/config-render` | 主题已合并或过时；待确认后 `git push origin --delete feat/config-render` |
| `feat/placeholder-catalog` | 主题已合并或过时；待确认后 `git push origin --delete feat/placeholder-catalog` |
| `fix/contributing-codeowners-links` | 主题已合并或过时；待确认后 `git push origin --delete fix/contributing-codeowners-links` |
| `rename/alertmanager-dir` | 主题已合并或过时；待确认后 `git push origin --delete rename/alertmanager-dir` |
| `rename/metrics-kubedns-files` | 主题已合并或过时；待确认后 `git push origin --delete rename/metrics-kubedns-files` |
| `scripts/cert-expiry` | 主题已合并或过时；待确认后 `git push origin --delete scripts/cert-expiry` |

## 主仓 — 保留

| 分支 | 理由 |
|------|------|
| `master` | 默认分支 |

## Wiki 远程 — 建议删除（已确认合入 Wiki master）

| 分支 | 理由 |
|------|------|
| `wiki/ia-restructure` | 已合入 Wiki master |
| `wiki/infra-consistency` | 已合入 Wiki master |
| `wiki/archive-banners` | 已合入 Wiki master |

## Wiki — 保留

| 引用 | 理由 |
|------|------|
| `master` | 现行 Wiki |
| tag `backup/wiki-master-20260727` | 合并前备份，禁止删除 |

## 确认后执行示例（勿在未批准时运行）

```bash
# 主仓（逐条确认）
# git push origin --delete <branch>
# Wiki
# git -C TomYang.wiki push origin --delete wiki/ia-restructure wiki/infra-consistency wiki/archive-banners
```

## 回滚

未删除则无需回滚；误删可用本地或 PR 分支引用恢复。
