# Wiki Task 6 跟踪

**Wiki 分支:** `wiki/infra-consistency`

合并：

```bash
cd TomYang.wiki
git fetch origin wiki/infra-consistency
git checkout master && git merge --ff-only origin/wiki/infra-consistency && git push
```

详情见 `docs/audits/wiki-repo-consistency.md`。
