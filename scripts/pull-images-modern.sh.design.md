# scripts/pull-images-modern.sh — 设计说明

本文是 [`docs/audits/pull-script-modernization.md`](../docs/audits/pull-script-modernization.md) 第 4 节的旁路副本，便于在 `scripts/` 下发现。

**状态:** 仅设计，**尚未**提供可执行 `pull-images-modern.sh`。
**禁止:** 覆盖仓库根目录 `pull.sh`。

完整审计背景、问题清单与接口草图见上述审计文档，请勿在两处维护互相矛盾的行为描述；若有歧义以 `docs/audits/pull-script-modernization.md` 为准。
