apiVersion: apps/v1
kind: Deployment
metadata:
  name: new
spec:
  selector:
    matchLabels:
      app: x
  template:
    metadata:
      labels:
        app: x
    spec:
      containers:
      - name: c
        image: nginx:1.27.5
